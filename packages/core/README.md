# @revertwtf/core

Shared types, hex helpers, and result models for [revert.wtf](https://revert.wtf) packages.

Pure TypeScript, no runtime dependencies, no I/O.

```bash
pnpm add @revertwtf/core
```

```ts
import { isHex, type Explanation } from "@revertwtf/core";
```
